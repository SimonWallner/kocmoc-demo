#include <gtest/gtest.h>

#include <Symbol.hpp>

using kocmoc::Symbol;
using kocmoc::symbolize;

TEST(DummyTest, TestOne)
{
	/*Symbol s1 = symbolize("foo");
	Symbol s2 = symbolize("bar");
	
	ASSERT_EQ(s1, s2);*/
	ASSERT_EQ(1, 1);
}